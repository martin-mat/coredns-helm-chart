apiVersion: v2
name: coredns
version: 1.48.0
appVersion: 1.14.6
home: https://coredns.io
icon: https://coredns.io/images/CoreDNS_Colour_Horizontal.png
description: CoreDNS is a DNS server that chains plugins and provides Kubernetes DNS Services
keywords:
  - coredns
  - dns
  - kubedns
sources:
  - https://github.com/coredns/coredns
maintainers:
  - name: mrueg
  - name: haad
  - name: hagaibarel
  - name: shubham-cmyk
type: application
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: The CoreDNS container runs as the image's non-root user (65532) with the RuntimeDefault seccomp profile by default, so the chart's defaults satisfy the restricted Pod Security Standard
